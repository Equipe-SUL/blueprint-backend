from django.contrib.auth.models import AbstractUser

class Usuario(AbstractUser):
	# Campos extras do usuario entram aqui.
	# Completem o model com os atributos já definidos no front.
	# E pelo que vi o abstract user já tem username, email, password. Então só precisa adicionar os campos extras.
	pass
